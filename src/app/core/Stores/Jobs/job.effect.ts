import { inject } from "@angular/core";
import { Actions, createEffect } from "@ngrx/effects";

// const JobEffect = createEffect(
//     (actions$ = inject(Actions), jobservice = inject(Job))    
// )